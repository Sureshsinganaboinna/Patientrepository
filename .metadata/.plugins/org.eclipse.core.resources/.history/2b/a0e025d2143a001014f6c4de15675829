package com.amazon.backend.service;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.beans.factory.annotation.Value;
import org.springframework.stereotype.Service;
import org.springframework.web.multipart.MultipartFile;

import com.amazon.backend.repository.UploadFileRepository;

import ch.qos.logback.core.util.StringUtil;
import io.micrometer.common.util.StringUtils;

@Service
public class UploadFileService {
	
	
	
	@Value("{file.upload.greetings}")
    private String uploadFilePath;
	
	private int uploadFileSize = 1024*1024*2;
	
	
	@Autowired
	private UploadFileRepository uploadFileRepository;
	
	public void uploadGreetingFile(MultipartFile inpFile)
	{
		
		String cleanedInputFile = StringUtils.cleanPath(inpFile.getOriginalFilename());
		String filenameextension = StringUtils.getFilenameExtension(cleanedInputFile);
		
		
		
		
		
	}
	
	
	
	
	

}
